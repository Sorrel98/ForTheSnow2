using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ethics_score : MonoBehaviour
{
    public static int ethicsScore = 0;
    public static int ethicsNum = 1;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
