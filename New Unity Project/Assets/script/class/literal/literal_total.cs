using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class literal_total : MonoBehaviour
{
    public static int literalScore = 0;
    public static int literalNum = 1;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
