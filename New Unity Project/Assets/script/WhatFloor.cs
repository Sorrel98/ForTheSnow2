using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhatFloor : MonoBehaviour
{
    public static int floor = 0;
    // Start is called before the first frame update
    void Awake()
    {
        floor = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
